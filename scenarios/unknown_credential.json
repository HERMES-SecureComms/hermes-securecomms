{
  "name": "unknown-credential",
  "steps": [
    {"action": "attach_credential", "credential_id": "UNKNOWN-001", "expect": {"auth_state": "UNAUTHENTICATED", "session_valid": false}},
    {"action": "ptt_press", "expect": {"decision": "DENY_UNKNOWN_CREDENTIAL", "tx_enabled": false}}
  ]
}

